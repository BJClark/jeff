---
name: bdd
description: Generate behaviour-focused tasks with Given/When/Then acceptance criteria from the story map and opportunities. Writes to .jeff/TASKS.md.
allowed-tools:
  - Skill
---

Invoke the `jeff:bdd` skill with `$ARGUMENTS`.

Workflow, acceptance-criteria rules, template, and examples live under `skills/bdd/`.
