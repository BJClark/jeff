---
name: map
description: Create or update a user story map (backbone, walking skeleton, ribs). Writes to .jeff/STORY_MAP.md.
argument-hint: "[backbone|skeleton|ribs]"
allowed-tools:
  - Skill
---

Invoke the `jeff:map` skill. Pass `$ARGUMENTS` through as the layer focus.

The skill's workflow, methodology, troubleshooting, and examples live under `skills/map/`.
