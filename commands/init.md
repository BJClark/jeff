---
name: init
description: Scaffold the .jeff/ directory with artifact templates. Run this first before other jeff commands.
argument-hint: "[project-name]"
allowed-tools:
  - Skill
---

Invoke the `jeff:init` skill. Pass `$ARGUMENTS` as the optional project name.

Workflow, edge cases, and examples live under `skills/init/`.
